#include "main.h"
/**
 * _isalpha - lldldl
 *
 * @c: any parameter
 *
 * Return: ldldlld
 */
int _isalpha(int c)
{
	if ((c >= 'A' && c <= 'Z') || (c >= 'a' && c <= 'z'))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
