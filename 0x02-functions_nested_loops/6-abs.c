#include <math.h>
#include "main.h"
/**
 * _abs -  returns the absolute value of a number
 *
 * @x: any integer
 *
 * Return: always return 0
 */
int _abs(int x)
{
	int y;

	y = sqrt(int x* *2);
	return (y);
}
