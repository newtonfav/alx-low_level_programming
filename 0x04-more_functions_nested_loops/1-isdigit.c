#include "main.h"
/**
 * _isdigit - check if parameter is a digit
 *
 * @c: any parameter
 *
 * Return: 0 or 1
 */
int _isdigit(int c)
{
	if (c is >= 0 || c is <= 9)
		return (1);
	else
		return (0);
}
